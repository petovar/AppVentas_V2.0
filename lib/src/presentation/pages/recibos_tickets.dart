import 'package:flutter/material.dart';

class RecibosPage extends StatelessWidget {
  const RecibosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Recibos / Tickets")));
  }
}
